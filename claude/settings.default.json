{
  "env": {
    "CLAUDE_CODE_SUBAGENT_MODEL": "opus"
  },
	"attribution": {
		"commit": "",
    "pr": "",
    "sessionUrl": false
	},
  "permissions": {
    "defaultMode": "auto",
    "additionalDirectories": [
      "/tmp"
    ]
  },
  "model": "opus",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cat ~/.claude/tropes.md"
          }
        ]
      }
    ]
  },
  "statusLine": {
    "type": "command",
    "command": "bash ~/.claude/statusline-command.sh"
  },
  "enabledPlugins": {
    "gopls-lsp@claude-plugins-official": true,
    "ponytail@ponytail": true
  },
  "extraKnownMarketplaces": {
    "ponytail": {
      "source": {
        "source": "github",
        "repo": "DietrichGebert/ponytail"
      }
    }
  },
  "effortLevel": "high",
  "skipDangerousModePermissionPrompt": true,
  "theme": "dark",
  "skipAutoPermissionPrompt": true,
  "autoMode": {
    "soft_deny": [
      "$defaults",
      "Bash(make remote-deploy:*) in autobutler-org/quark — deploy target unspecified, treat as production-adjacent until confirmed"
    ],
    "environment": [
      "### Org-wide",
      "**Organization**: autobutler-org (GitHub) — scoped to the autobutler-org/quark repo",
      "**Cloud provider(s)**: Azure (AZURE_STORAGE_SAS_TOKEN referenced in CI secrets)",
      "**Repository visibility**: PUBLIC — github.com/autobutler-org/quark (confirmed via gh)",
      "**Internal sharing / snippet hosting**: None configured — treat public paste/gist services as outside the trust boundary",
      "**Secrets management**: CI secret AZURE_STORAGE_SAS_TOKEN (name only, not value); .env / .env.example used locally for Google OAuth config (gitignored)",
      "**Default / protected branches**: main (default, protected via active ruleset `Default`, confirmed via gh)",
      "**CI/CD deploy targets**: GitHub Actions workflows (ci.yml, check.yml, codeql.yml); Makefile has `remote-deploy` target — target host/environment not specified",
      "**Network posture**: None configured",
      "**Source control**: The autobutler-org/quark repo and its origin remote only (github.com:autobutler-org/quark) — no additional orgs configured",
      "**Trusted internal domains**: None configured",
      "**Trusted cloud buckets**: None configured",
      "**Key internal services**: None configured",
      "**Internal package registry**: None configured",
      "**Sensitive data locations & audiences**: .env and .env.example (Google OAuth client ID/secret); any file or store holding personal data (this project handles user photos/files/documents as a personal cloud product), confidential business data, credentials, regulated data, or similarly sensitive material; preserve exact handles when known and share only with audiences cleared at the [named+specifics] bar",
      "**Data retention / declassification**: None configured",
      "**Sensitive remote targets**: any namespace, host, or container whose name carries `prod` or `production` as a whole word or name segment (hyphen/underscore/dot-delimited)",
      "**Protected deployment namespaces / environments**: None configured — fall back to the Sensitive remote targets heuristic",
      "**Protected IaC scopes**: IAM, RBAC, networking, quota, and node-pool resources; anything whose name or tag carries `prod` or `production` as a whole word or name segment",
      "### User-specific",
      "**Primary use of Claude Code**: software development (self-hosted personal cloud project — Go/Gin backend, Flutter frontend)",
      "**Trusted repo**: /Users/jamesaorson/github.com/autobutler-org/quark and its origin remote (github.com:autobutler-org/quark) — repo is PUBLIC, so only this repo's own work is safe to push there; secrets and sensitive data are never cleared into it by visibility alone",
      "**Org-specific CLIs**: None configured (project uses go, flutter, make, sqlc, swag, air per README; `ralph` and `az` seen in shell history but not corroborated as org-specific in this repo)",
      "routine build/test/lint (`make build`, `make check`, `make test`, `make generate`) under this repo's working directory"
    ]
  },
  "gitAttribution": false
}
